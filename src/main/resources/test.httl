hi, ${msg}
test list:
#for(i : list)
    list: ${i}
#end
test object list:
#for(others.Bean o : objList)
    objlist: ${o.msg}
#end
test if:
#if(isTest)
    test traverse obj:
    #for(entry : ["red":"#FF0000","yellow":"#00FF00"])
    ${entry.key} = ${entry.value}
    #end
#end